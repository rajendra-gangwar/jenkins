pipeline {
    agent any
    stages {
        stage('Running python Script') {
            steps {
                sh 'python3 test.py ${instance_id} ${instance_type}'
            }
        }
       }
    }
