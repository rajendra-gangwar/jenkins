#!/usr/bin/env groovy
stages {
        stage('Change instance type') {
            steps {
                println ("$instance_type")
                println ("$instance_id")
                echo "Hello"
            }
        }
    }
