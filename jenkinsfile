pipeline {
    agent any
    stages {
        stage('Running python Script') {
            steps {
                sh 'python3 aws.py ${instance_id} ${instance_type}'
            }
        }
       }
    }
