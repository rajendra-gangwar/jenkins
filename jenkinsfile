pipeline {
    agent any }
    stages {
        println ("$instance_type")
        println ("$instance_id")
        stage('Change instance type') {
            steps {
                echo "Hello"
            }
        }
    }
